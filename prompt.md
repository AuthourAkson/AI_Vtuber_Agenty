请帮我重构‘Multi-Agent’页面的布局
===================

**1. 状态管理与连接逻辑 (Backend Bridge)：**

* 创建一个 `AgentManager` 类（建议使用 Provider/Riverpod），用于管理：
  
  * **局域网状态：** 存储 `Host IP`、`Port` (默认9090) 和 `Topic`。
  
  * **连接控制：** 实现 `joinLAN` 和 `createLAN` 的 Socket/HTTP 逻辑。
  
  * **Agent 列表：** 维护一个从后端拉取的 `AgentModel` 动态列表。

**2. 嵌套导航与二级侧边栏 (Ref: 图1, 图3)：**

* 在“Multi-Agent”主页面内，实现一个二级侧边栏：
  
  * **模式切换：** 提供两个图标按钮（会话、通讯录）。
  
  * **会话模式：** 显示 Agent 聊天列表，上方带搜索框。若未选 Agent，右侧显示“选择一个会话开始对话”的空状态占位符。
  
  * **通讯录模式：** 显示已创建的 AI 员工列表，底部包含一个“+ 创建员工”按钮。

**3. 局域网设置中心 (Ref: 图2)：**

* 在主设置界面中新增“局域网”模块：
  
  * 采用分栏设计：左侧为设置项菜单，右侧为配置内容。
  
  * 配置页包含：加入/创建 Tab 切换、IP 文本框、端口框、Topic 分组标识框。
  
  * 当全局状态 `isConnected` 为 false 时，在 Multi-Agent 页面顶部显示浮动的橙色提示条：“未加入局域网，无法远程通信”，点击可直达此设置页。

**4. 员工创建表单与逻辑 (Ref: 图4)：**

* 实现“创建员工”对话框（Dialog）：
  
  * **基础信息：** 名称（TextField）、描述（多行 TextField）。
  
  * **设备/模型分配：** 提供两个 `DropdownButtonFormField`。其中“分配设备”包含选项：自动分配、[已发现设备列表]；“AI 模型”包含：使用默认模型、[模型枚举]。
  
  * 点击“创建”时，调用后端接口并将新 Agent 实时同步至“通讯录”列表。

**5. 聊天窗口集成 (Ref: 图1)：**

* 在 Multi-Agent 选定 Agent 后的主内容区，集成标准聊天 UI：
  
  * 支持历史消息滚动。
  
  * 底部发送栏需与现有项目的 `Input` 模块逻辑对接。

**技术约束：**

* **布局逻辑：** 使用 `Row` 和 `Expanded` 确保在桌面端窗口拉伸时，三栏布局（主导航-二级列表-内容区）能自适应缩放。

* **视觉风格：** 严格遵循图5的深色背景（Background: #121212 左右）和圆角规范。

* **代码组织：** 请将 UI 组件与逻辑 Service 分离，提供清晰的文件夹结构建议。
